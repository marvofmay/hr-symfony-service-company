services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\Module\System\Domain\Factory\ImporterFactory:
      arguments:
        $importSharedValidators: !tagged_iterator 'app.import_shared_validator'
        $importEmployeesValidators: !tagged_iterator 'app.import_employees_validator'
